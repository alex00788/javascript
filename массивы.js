
cars.push()    // чтоб добавить в конец массива переменную
// если функция push вызываеться у объекта cars то это МЕТОД
cars.unshift() // добавляет в начало
cars.shift()   // удолит 1й элемент
cars.pop ()    // удолит последн

cars.reverse()  // переворачивает
text.split   // делет строчку и возвращ массив

const text = 'привет, друзья '
const reverseText = text.split ('').reverse().join(' ')     //разворот массива
